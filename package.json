{
  "name": "nullframe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4321 -d site",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
